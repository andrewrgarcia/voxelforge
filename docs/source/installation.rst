Installation
============

**VoxelForge** can be easily installed using pip. Ensure you have Python and pip installed on your system, then run the following command:

.. code-block:: bash

    pip install VoxelForge

This command will download and install VoxelForge and all required dependencies.
